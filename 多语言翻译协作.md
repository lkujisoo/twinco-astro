# 多语言翻译协作（Claude ⇄ Codex）

> 这份文档是 Claude 和 Codex 两个 agent 一起做 TWINCO 网站多语言的**工作交流单**。
> 双方都读它、都更新它。做任何翻译前，先读第 2~4 节；开始动工前，认领第 8 节状态表里的语言。

本地服务器：`npm run dev` → http://localhost:4321/ （已由 Claude 启动，保持开着边做边看）

---

## 1. 目标

现在 zh（中文）和 en（英文）已完成。本轮要新增 **7 种语言**：

`fr` 法语 · `de` 德语 · `da` 丹麦语 · `es` 西班牙语 · `it` 意大利语 · `ja` 日语 · `ko` 韩语

原则（沿用现有设计，**不改**）：**不做语言回退**。一种语言必须 100% 翻完并验证通过，才把它加进 `ENABLED_LOCALES` 露出去；没翻完的语言不生成页面、不进语言切换菜单，从结构上杜绝“中英/中X 混排”的页面。

---

## 2. 架构速览：翻译只碰这 3 类文件

| # | 内容 | 路径 | 数量/语言 | 说明 |
|---|---|---|---|---|
| ① | 界面文案 | `src/i18n/ui/<lang>.json` | **143 个 key** | 导航/页脚/按钮/静态页文字。key 必须和 `ui/zh.json` **完全一致** |
| ② | 分类名称 | `src/i18n/categories/<lang>.json` | **49 个 key** | 9 大类 + 子类的名字。key 是 `大类id` 或 `大类id.子类id` |
| ③ | 产品内容 | `src/content/products/<lang>/<id>.md` | **106 个文件** | 每个产品一个 md，结构镜像 `products/zh/<id>.md` |

单一真源：`src/i18n/locales.mjs`。语言清单、显示名、html lang、书写方向都在这里，**不要在别处硬编码语言列表**。

翻译源与参照：
- **源（权威）= `zh`**。以中文文件的字段和取值为准。
- **参照 = `en`**：英文那一套已完整翻好，是“一个产品翻译完成后长什么样”的**参考实现**。做欧洲语言（fr/de/da/es/it）时，直接以 `en/<id>.md` 为翻译底本更自然；做 ja/ko 时 zh/en 都可参考。
- 若 zh 和 en 内容有出入，以 **zh** 为准（那是用户维护的真源）。

---

## 3. 路由与页面切换逻辑（务必对齐）

配置：`astro.config.mjs` 里 `prefixDefaultLocale: false`。

- **默认语言 zh 不带前缀**：`/`、`/products`、`/about`、`/contact`、`/product/<id>`
- **其他语言带前缀**：`/<lang>/...`，如 `/fr/`、`/fr/products`、`/fr/about`、`/fr/contact`、`/fr/product/3177`

页面来源：
- `src/pages/*`（无 `[lang]`）= 中文默认页。
- `src/pages/[lang]/*` = 其他语言页，靠 `getStaticPaths()` 生成。它读 `nonDefaultLocalePaths()`（= `ENABLED_LOCALES` 去掉默认语言）和产品 id 来决定要生成哪些页。

语言切换器（`src/layouts/BaseLayout.astro` + `getLocaleOptions`）：
- **只列 `ENABLED_LOCALES` 里的语言**。所以每 enable 一种语言，切换菜单就自动多一项，无需改模板。
- 切换时用 `stripLangPrefix` 去掉当前前缀、再 `localizePath` 加上目标语言前缀，**保持当前所在页面**（在 `/fr/product/3177` 点“English”会去 `/en/product/3177`）。

关键约束（决定了“完成标准”）：
- 产品详情页 `[lang]/product/[id]` 是遍历 `getProductsByLang(lang)` 生成的。**某语言缺某个产品 md → 该语言就没有这个产品页**，在别的语言的该产品页上切到它会 404。
- 所以每种要 enable 的语言**必须凑齐全部 106 个产品**，界面 143 key、分类 49 key 一个都不能少。
- `useTranslations`/`getCategories` 缺 key 会**直接抛错让 build 失败**（这是刻意的护栏）。因此“翻一半就 enable”会直接把构建搞挂。

---

## 4. 翻译规则（逐字段 + 冻结清单）

### 4.1 要翻译的字段
- 产品：`name`（**只翻描述性词，型号号码/品牌名 TWIN/TWINCO 保留**）、`subtitle`、`description`、`material`、`features[]`、`highlights[].title`、`highlights[].text`、`infoSections[].title`、`infoSections[].content[]`、`colors[].name`、`detailImages[].caption`、`optionProducts[].{name(描述部分),subtitle,description,features[],specs[].label}`、`modelItems[].{title,description}`、`accessoryButtons[].label`、`addons[].label`、md 正文（`---` 之后那段）。
- 界面 `ui/*.json`、分类 `categories/*.json`：全部值都翻。

### 4.2 【冻结】绝对不要翻译 / 必须逐字复制的字段
这些一旦改就会把图片、SKU、切换逻辑弄坏：

- **所有 key、id、slug**：`id`、`category`、`subcategory`、`colors[].key`（运行时按位置从 en 补，不在 md 里）、`accessoryButtons[].key`、`addons[].key`、`stepVariants[].colorImages`/`colorSkus`/`addonImages` 的**键名**、`categories.json` 的 key。
- **所有图片路径**：`defaultImage`、`image`、`imageWheels`、`imageHandrail`、`imageBoth`、`highlights[].image`、`detailImages[].src`、`lifestyleImages[]`、`stepVariants[].image`、`catalogImages[].src`。逐字复制 zh 的路径。
- **所有 SKU / 型号号码**：`sku`、`colorSkus` 的值、`name` 里的型号数字。
- **纯数字/单位/规格**：`dimensions`、`maxLoad`、`hex`、`ral`、`gridVariants` 的行列 label、`stepVariants[].label`（多为 "460 mm"、"3 steps" 这种，只有含真单词时才翻，如 "steps"→各语言的"档/级"）、`specs[].value`（多为尺寸数字）。
- **`colors[].name` 的特殊处理**：色名要翻（White→Blanc/Weiß…），但**运行时靠 en 色名当 key 查图**（见 `product.ts` 的 `attachRefs`），所以**颜色数组的顺序、条数必须和 en 完全一致**，只改 `name` 文本，别增删别调序。

> 判断口诀：**「这串字符会不会被用来查文件或拼 URL？」** 会 → 冻结，逐字复制。只是给人看的文字 → 翻。

### 4.3 语言风格
- 用目标语言的**母语行文**，不是直译腔。ja/ko 用敬体（です・ます／합니다 体）。
- 保留原文的换行/占位符：`{n}` `{q}` `{name}` `{id}` 原样保留，别翻别删（`home.heroAlt` 里的 `{n}` 等）。
- 数字用半角，度数 `°`、毫米 `mm`、`kg` 等单位保持不变。


---

## 5. 一种语言的完整流程（做完才算“完成”）

对每种语言 `<lang>`：

1. **界面**：复制 `src/i18n/ui/zh.json` → `src/i18n/ui/<lang>.json`，逐值翻译（143 key，key 不动）。
2. **分类**：复制 `src/i18n/categories/zh.json` → `src/i18n/categories/<lang>.json`，逐值翻译（49 key，key 不动）。
3. **产品**：为全部 106 个 id 生成 `src/content/products/<lang>/<id>.md`，以 `en/<id>.md`（或 zh）为底本，按第 4 节规则翻译，**冻结字段逐字复制**。
4. **登记**（两处，缺一构建就跑不起来）：
   - `src/i18n/index.ts`：`import xx from './ui/<lang>.json'` 并加进 `dictionaries`。
   - `src/i18n/categories.ts`：`import xx from './categories/<lang>.json'` 并加进 `names`。
5. **enable**：把 `<lang>` 加进 `src/i18n/locales.mjs` 的 `ENABLED_LOCALES`。
6. **验证**（第 7 节）：`npm run build` 必须 0 error；抽查页面渲染正常、切换器出现该语言、图片不裂。
7. 更新第 8 节状态表。

> `LOCALES`、`LOCALE_LABELS`、`LOCALE_HTML_LANG`、`LOCALE_DIR` 里 7 种语言**已经预置好了**，不用动。只动 `ENABLED_LOCALES`。

---

## 6. 分工

按语言切块，各自独立、互不踩踏（不同语言 = 不同文件夹/不同 json，天然无冲突）。

| Agent | 负责语言 | 备注 |
|---|---|---|
| **Codex** | `fr` 法语、`de` 德语、`es` 西班牙语、`it` 意大利语 | 4 种拉丁语系，可参照 `en` 底本，风格接近好批量 |
| **Claude** | `da` 丹麦语、`ja` 日语、`ko` 韩语 | da 是品牌母语要地道；ja/ko 敬体 + CJK 断句，我来把控 |

协作纪律：
- **一次一种语言，做完一整套再 enable**，不要多语言半成品同时 enable。
- **谁翻的语言谁 enable**（改 `ENABLED_LOCALES` / `index.ts` / `categories.ts` 那 3 处登记）。这 3 个文件是唯一的交叉点 —— **改之前在第 8 节状态表把该行标成「登记中」，改完立刻 `git`/保存并标「已enable」**，避免两人同时改同一文件互相覆盖。
- 若两人同一时段都要改 `index.ts`/`categories.ts`：以状态表为准排队，一次一个人改。改动只是「加一行 import + 往对象里加一项」，很小，串行几秒钟即可。
- 有疑问、发现规则漏洞、或某产品源数据本身有问题 → 写到第 9 节「问题与决议」，别自己拍板改冻结字段。

---

## 7. 验证清单（enable 前必须过）

```bash
# 构建：缺 key / 缺产品 / 语法错都会在这里炸出来
npm run build
```

- [ ] `npm run build` 退出码 0，无 `[i18n] ... 缺少` 报错。
- [ ] `src/content/products/<lang>/` 正好 106 个 md，id 集合和 zh 一致。
- [ ] `ui/<lang>.json` 143 key、`categories/<lang>.json` 49 key，key 与 zh 逐一对应（无多无少）。
- [ ] dev 站抽查：`/<lang>/`、`/<lang>/products`、`/<lang>/about`、`/<lang>/contact`、`/<lang>/product/3177` 都 200 且无混排。
- [ ] 语言切换器出现该语言；在该语言产品页切到 en/zh 能停在同一产品。
- [ ] 图片、颜色切换、型号筛选正常（= 冻结字段没被误翻）。

快速核对 key 是否齐（PowerShell 或 bash 皆可，示意）：
```bash
node -e "const z=require('./src/i18n/ui/zh.json'),x=require('./src/i18n/ui/<lang>.json');const a=Object.keys(z),b=new Set(Object.keys(x));console.log('缺:',a.filter(k=>!b.has(k)));console.log('多:',Object.keys(x).filter(k=>!new Set(a).has(k)))"
```

---

## 8. 语言状态表（动工前认领，做完更新）

状态取值：`未开始` → `翻译中` → `待验证` → `登记中` → `已enable`

| 语言 | 负责 | 界面143 | 分类49 | 产品106 | 登记(index/categories) | ENABLED | 状态 |
|---|---|---|---|---|---|---|---|
| zh | — | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable（默认） |
| en | — | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable |
| fr | Codex | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable |
| de | Codex | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable |
| es | Codex | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable |
| it | Codex | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable |
| da | Claude | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable |
| ja | Claude | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable |
| ko | Claude | ✅ | ✅ | ✅ | ✅ | ✅ | 已enable |

---

## 9. 问题与决议（谁发现谁记，别默默改冻结字段）

- （示例）_yyyy-mm-dd Codex：产品 XXXX 的 zh 和 en 的 features 条数不一致，以 zh 为准，en 少一条 → 记录，翻 fr 时按 zh 补齐。_
- 2026-08-27 Codex：ja / ko 译稿里残留少量英文标签（如 `Mounts on top`、`shelf width`、`Board`、`Unfolded`），已逐项清理并通过 `npm run build` 验证。

---

## 10. 关键文件索引

- 语言真源：`src/i18n/locales.mjs`
- 界面翻译函数/登记：`src/i18n/index.ts`（`dictionaries`）
- 分类翻译函数/登记：`src/i18n/categories.ts`（`names`）
- 产品读取（含 colors key 补齐逻辑）：`src/i18n/product.ts`
- 语言切换器 + 客户端注入：`src/layouts/BaseLayout.astro`
- 其他语言页壳：`src/pages/[lang]/`
- 内容 schema（字段定义）：`src/content.config.ts`
- 既有翻译缺口分析：`TRANSLATION_TODO.md`、`src/i18n/_source/translation-todo.json`
